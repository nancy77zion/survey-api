<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>

        h1{
            margin-top: 20%;
        }
        input{
            width: 98%;
            height: 27px;
            border-radius: 7px;
            border: 1px solid blue;
            padding: 5px;
            margin: 10px 0px;

        }
        form{
            width: 30%;
            padding: 15px;
            /* border: 1px solid red; */
            border: 0.5px solid #4862be;
            height: 500px;
            box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.5);
            position: absolute;
            transform: translate(100%, 50%);
        }
        button{
            width: 100%;
            height: 40px;
            background-color: #4862be;
            border: none;
            border-radius: 7px;
            color: white;
        }
        body{
            position: relative;
        }
        label{
            display: block;
            margin-top: 20px;
            font-size: large;
        }
    </style>
    <script>
          var status = '<%- JSON.stringify(status) %>';

      if (JSON.parse(status) == "verified") {
        if (confirm("Do u want to login?")) {
          window.location.href = "https://conclasesurvey.app/vercel.app/"; //need the login url
        }
      }
    </script>
  </head>
  <body>
   
    <form action="" method="post">
        <h1><%= email%></h1>
        <label for="password">Enter your new password</label>
        <br/>
      <input
        type="password"
        name="password"
        id="password"
        placeholder="password"
      /><br />
      <button type="submit" value="submit">Submit</button>
    </form>
  </body>
</html>